const city = "Moscow";      //Создали переменную для хранения моего города
const temperature = -7;       //Создали переменную для хранения температуры на сегодняшний день

function greeting(city, temperature) {
    return `Hello, today in ${city}: ${temperature}`;       //Написали функцию, которая возвращает мне надпись, беря значения из переменных
}

console.log(greeting(city, temperature));     //Вывели результат на консоль



const result = (3 + 4) * 10;   //Написали выражение, которое вычисляет сумму двух чисел и умножает результат на третье число

console.log(result);          //Вывели результат на консоль  